name: typescript-checks
on:
  pull_request:
    paths:
      - '.github/workflows/typescript-tests.yml'
      - '.eslintrc.js'
      - '**.ts'
      - '**.tsx'
      - 'optuna_dashboard_client/package.json'
      - 'optuna_dashboard_client/package-lock.json'
      - 'optuna_dashboard_client/tsconfig.json'
jobs:
  lint:
    name: Lint checking on Ubuntu
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@master

      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - run: npm install
      - run: npm run lint

  build:
    name: JS build check
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@master

      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Build bundle.js
        working-directory: optuna_dashboard_client
        run: |
          npm install
          npm run build:dev
          npm run build:prd

  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@master

      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Test
        working-directory: optuna_dashboard_client
        run: |
          npm install
          npm run test
